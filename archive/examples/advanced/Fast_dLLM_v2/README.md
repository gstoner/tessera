# Archived Fast-dLLM implementation sketches

This directory preserves the original placeholder C++ source drop and the
confidence/KV-cache runtime-policy pseudocode. They are not compiled or used by
the active example.

The maintained reference and compiler smoke live in
`examples/advanced/Fast_dLLM_v2/`.
