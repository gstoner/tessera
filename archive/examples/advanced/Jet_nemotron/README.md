# Archived Jet-Nemotron research port

This directory preserves the incomplete full-model/PostNAS port. It depends on
an unshipped `tessera.stdlib`, contains ghost-package imports and placeholder
runtime surfaces, and is not active compiler or execution evidence.

The maintained D=128 linear-attention smoke is
`examples/advanced/Jet_nemotron/tests/smoke_linear_attention.py`.
