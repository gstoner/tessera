# Archived

The contents of this folder have been moved to `docs/old_concepts/Tracing_JIT_Research/`.

Tessera uses a static AOT compilation pipeline (Graph IR → Schedule IR → Tile IR → Target IR).
The tracing JIT design was exploratory research and is not part of the current roadmap.

