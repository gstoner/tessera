# Stub / completeness surface — software-actionable worklist

> Generated by `scripts/stub_surface_report.py` from the checked-in audit
> dashboards. Hardware-gated rows (NVIDIA/ROCm/TPU/... needing real silicon)
> are separated out — they are expected-incomplete, not stubs. **Rerun to
> refresh; do not hand-edit.**

## P0 — verifier stubs (verify() declared but no-ops)

ODS ops: **73 real**, **9 trivial_stub**, **65 no_verifier**, 0 absent.

**9 trivial-stub verifiers (definite stubs — close these):**
  - `ArchGumbelSoftmaxOp` (TesseraOps.td)
  - `ArchHardConcreteOp` (TesseraOps.td)
  - `ArchMixedOp` (TesseraOps.td)
  - `ArchParameterOp` (TesseraOps.td)
  - `ArchSTEOneHotOp` (TesseraOps.td)
  - `ArchSwitchOp` (TesseraOps.td)
  - `ArchWeightedSumOp` (TesseraOps.td)
  - `KVCacheCreateOp` (TesseraOps.td)
  - `RingCreateOp` (TesseraOps.td)

_65 no-verifier ops need manual triage_ (many — pure elementwise — legitimately need none; control/collective/reshape-shaped ops should get one). Representative:
  `ALiBiOp`, `AdafactorOp`, `AdamOp`, `AdamWOp`, `AddOp`, `AllGatherOp`, `AllReduceOp`, `CachePageLookupOp`, `ControlForOp`, `ControlIfOp`, `ControlWhileOp`, `CustomAdjointCallOp`, …

## P1 — software conformance gaps (op×target proof ladder)

42 op×target cells: {'partial': 14, 'missing': 23, 'complete': 5}.

First-failing gate: {'hardware_smoke': 14, 'numerical': 2, 'codegen': 7, 'toolchain': 10, 'link': 4}.

**Software-actionable (9** stop at ['codegen', 'numerical']**) — real lowering/codegen gaps to fix:**
  - `conv2d` → `cpu` (stops @ numerical)
  - `conv2d` → `metalium` (stops @ codegen)
  - `conv2d` → `nvidia` (stops @ codegen)
  - `conv2d` → `rocm` (stops @ codegen)
  - `flash_attn` → `metalium` (stops @ codegen)
  - `kv_cache_read` → `cpu` (stops @ numerical)
  - `kv_cache_read` → `metalium` (stops @ codegen)
  - `kv_cache_read` → `nvidia` (stops @ codegen)
  - `kv_cache_read` → `rocm` (stops @ codegen)

_Hardware-gated (28 stop at ['hardware_smoke', 'link', 'toolchain']) — expected; need real silicon, not code._

## P2 — thin-test tail (the differential-generator target, item #2)

435 ops: {'structural_only': 184, 'covered_by_family': 124, 'needs_direct_test': 123, 'hardware_gated': 4}.

**123 `needs_direct_test`** + **118 zero-ref** ops — the long tail a generative differential harness (tracer vs numpy) covers far more cheaply than hand-written tests.

Zero-ref sample: `aot_export`, `aot_load`, `associative_scan`, `autocast`, `axis_index`, `axis_name`, `axis_size`, `calibration_observer`, `check_cauchy_riemann`, `checkpoint`, `clifford_codiff`, `clifford_conjugate`, `clifford_exp`, `clifford_ext_deriv`, `clifford_grade_involution`, `clifford_grade_projection`, `clifford_hodge_star`, `clifford_inner`, `clifford_integral`, `clifford_left_contraction`, …

## Takeaway

- The compiler is **not stub-riddled**: the actionable surface is the trivial-stub verifiers + a handful of software conformance cells; everything else is hardware-gated (honest) or thin-test (generative).
- **#4 (IR round-trip + fuzz)** hardens the parser/printer/verifier across all ops cheaply, no oracle needed.
- **#2 (differential generator)** is the right tool for the ~123 needs-direct-test ops + miscompile detection.

