# Stub / completeness surface — software-actionable worklist

> Generated by `scripts/stub_surface_report.py` from the checked-in audit
> dashboards. Hardware-gated rows (NVIDIA/ROCm/TPU/... needing real silicon)
> are separated out — they are expected-incomplete, not stubs. **Rerun to
> refresh; do not hand-edit.**

## P0 — verifier stubs (verify() declared but no-ops)

ODS ops: **163 real**, **0 trivial_stub**, **11 no_verifier**, 0 absent.

**0 trivial-stub verifiers (definite stubs — close these):**

_11 no-verifier ops need manual triage_ (many — pure elementwise — legitimately need none; control/collective/reshape-shaped ops should get one). Representative:
  `ArchSTEOneHotOp`, `NeighborsHaloExchangeOp`, `NeighborsHaloPackOp`, `NeighborsHaloRegionOp`, `NeighborsHaloTransportOp`, `NeighborsHaloUnpackOp`, `NeighborsNeighborReadOp`, `NeighborsPipelineConfigOp`, `NeighborsStencilApplyOp`, `NeighborsStencilDefineOp`, `NeighborsTopologyCreateOp`, …

## P1 — software conformance gaps (op×target proof ladder)

35 op×target cells: {'partial': 7, 'complete': 20, 'missing': 8}.

First-failing gate: {'hardware_smoke': 14, 'toolchain': 14}.

**Software-actionable (0** stop at []**) — real lowering/codegen gaps to fix:**

_Hardware-gated (28 stop at ['hardware_smoke', 'toolchain']) — expected; need real silicon, not code._

## P2 — thin-test tail (the differential-generator target, item #2)

480 ops: {'directly_tested': 363, 'structural_only': 87, 'covered_by_family': 26, 'hardware_gated': 4}.

**0 `needs_direct_test`** + **81 zero-ref** ops — the long tail a generative differential harness (tracer vs numpy) covers far more cheaply than hand-written tests.

Zero-ref sample: `aot_export`, `aot_load`, `associative_scan`, `autocast`, `axis_index`, `axis_name`, `axis_size`, `calibration_observer`, `check_cauchy_riemann`, `checkpoint`, `clifford_codiff`, `clifford_exp`, `clifford_ext_deriv`, `clifford_hodge_star`, `clifford_integral`, `clifford_log`, `clifford_vec_deriv`, `conformal_jacobian`, `custom_batching`, `custom_call`, …

## Takeaway

- The compiler is **not stub-riddled**: the actionable surface is the trivial-stub verifiers + a handful of software conformance cells; everything else is hardware-gated (honest) or thin-test (generative).
- **#4 (IR round-trip + fuzz)** hardens the parser/printer/verifier across all ops cheaply, no oracle needed.
- **#2 (differential generator)** is the right tool for the ~0 needs-direct-test ops + miscompile detection.

