# Stub / completeness surface — software-actionable worklist

> Generated by `scripts/stub_surface_report.py` from committed audit
> dashboards and the live test-coverage renderer. Hardware-gated rows (NVIDIA/ROCm/TPU/... needing real silicon)
> are separated out — they are expected-incomplete, not stubs. **Rerun to
> refresh; do not hand-edit.**

## P0 — verifier stubs (verify() declared but no-ops)

ODS ops: **236 real**, **0 trivial_stub**, **0 no_verifier**, 0 absent.

**0 trivial-stub verifiers (definite stubs — close these):**

_0 no-verifier ops need manual triage_ (many — pure elementwise — legitimately need none; control/collective/reshape-shaped ops should get one). Representative:
  , …

## P1 — software conformance gaps (op×target proof ladder)

63 op×target cells: {'reference': 11, 'complete': 31, 'missing': 21}.

First-failing gate: {'backend_compile': 32}.

**Software-actionable: 0** (stops at []) — real lowering/codegen gaps to fix:

_Target-environment-gated: 21 (stops at ['backend_compile']) — expected; requires the target toolchain and/or silicon proof lane._

## P2 — thin-test tail (the differential-generator target, item #2)

520 ops: {'directly_tested': 414, 'structural_only': 76, 'covered_by_family': 26, 'hardware_gated': 4}.

**0 `needs_direct_test`** + **70 zero-ref** ops — the long tail a generative differential harness (tracer vs numpy) covers far more cheaply than hand-written tests.

Zero-ref sample: `aot_export`, `aot_load`, `associative_scan`, `autocast`, `axis_index`, `axis_name`, `axis_size`, `calibration_observer`, `check_cauchy_riemann`, `checkpoint`, `clifford_codiff`, `clifford_exp`, `clifford_ext_deriv`, `clifford_hodge_star`, `clifford_integral`, `clifford_log`, `clifford_vec_deriv`, `conformal_jacobian`, `custom_batching`, `custom_call`, …

## Takeaway

- **No definite verifier stubs or software-actionable curated conformance gaps remain.** The open surface is no-verifier triage, target-environment proof, and thin-test hardening.
- **#4 (IR round-trip + fuzz)** hardens the parser/printer/verifier across all ops cheaply, no oracle needed.
- **#2 (differential generator)** is the right tool for the 0 needs-direct-test ops + miscompile detection.

