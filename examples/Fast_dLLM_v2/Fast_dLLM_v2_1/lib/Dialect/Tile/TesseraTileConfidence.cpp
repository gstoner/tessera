#include "mlir/IR/Builders.h"
#include "mlir/IR/BuiltinTypes.h"
#include "mlir/IR/OpImplementation.h"

// This file would normally be auto-generated via TableGen; here we provide
// tiny manual hooks/stubs so the project can compile while you wire the build.

namespace tessera { namespace tile {

// Placeholder stubs: real implementations will be generated or hand-written
// to support parsing/printing and verifiers.
struct ConfidenceStatsOp {}; // stub
struct PrefixLCPOp {};       // stub

}} // namespace
