# Tessera mypy baseline (2026-05-19)
#
# Captured count of pre-existing type errors at the moment ruff +
# mypy landed.  The validation spine uses this as a **ratchet**:
#
#   * `scripts/validate.sh` runs mypy and counts the reported errors.
#   * If the count is `<= BASELINE`, the script passes.
#   * If the count is `> BASELINE`, the script fails and prints the new
#     errors.
#
# Historical baseline: 357 errors, clustered in 6 files —
# nn/layers.py (71), checkpoint.py (58), quantization.py (52),
# ga/multivector.py (43), server.py (34), nn/module.py (28), plus a
# long tail across ~30 other files.  All cleared in the 2026-05-19
# mypy-backlog-cleanup sprint via PEP 526 slot annotations, cast()
# narrowing for Mapping[str, object]→narrower types, and tightening
# of variable annotations.  The ratchet now defends 0.
#
# Update this file after a focused cleanup sprint by re-running the
# baseline command:
#
#   ./scripts/mypy_ratchet.sh --update-baseline
#
BASELINE=0
